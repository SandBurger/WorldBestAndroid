<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@+id/action_adjust"
        android:title="수정"
        android:icon="@drawable/ic_trash_can"/>
    <item android:id="@+id/action_remove"
        android:title="삭제"
        android:icon="@drawable/ic_trash_can"/>
</menu>